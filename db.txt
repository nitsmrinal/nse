use nseinfo

CREATE OR replace TABLE nsefobhav
(
INSTRUMENT  VARCHAR(40) NOT NULL,
SYMBOL		VARCHAR(40) NOT NULL,
EXPIRY_DT 	DATE NOT NULL DEFAULT '2020-01-01',
STRIKE_PR  	DECIMAL(10,2) NOT NULL,
OPTION_TYP  VARCHAR(40) NOT NULL,
OPEN  		DECIMAL(10,2) NOT NULL,
HIGH		DECIMAL(10,2) NOT NULL,
LOW			DECIMAL(10,2) NOT NULL,
CLOSE		DECIMAL(10,2) NOT NULL,
SETTLE_PR   DECIMAL(10,2) NOT NULL,
CONTRACTS  	BIGINT	 NOT NULL,
VAL_INLAKH  DECIMAL(10,2) NOT NULL,
OPEN_INT 	BIGINT	 NOT NULL,
CHG_IN_OI	BIGINT	 NOT NULL,
TIMESTAMP	DATE NOT NULL DEFAULT '2020-01-01',
ERROR       VARCHAR(40)
)
alter table nsefobhav
ADD CONSTRAINT UNIQUE_KEYS_all UNIQUE (INSTRUMENT,SYMBOL,EXPIRY_DT,STRIKE_PR,OPTION_TYP,OPEN,HIGH,LOW,CLOSE,SETTLE_PR,CONTRACTS,VAL_INLAKH,OPEN_INT,	CHG_IN_OI,TIMESTAMP);


select * from nsefobhav

